<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developer.aaleen.passwordgenerator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_password_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_password_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SlashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".LoginActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".SettingsPref"
            android:screenOrientation="portrait"/>
        <activity android:name=".SavedPassword"
            android:screenOrientation="portrait"/>
    </application>

</manifest>